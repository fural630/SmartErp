<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.smartErp.system.dao.DictionaryTypeDao">  
    <select id="getDictionaryByParentId" resultType="hashMap" parameterType="int">  
        select id as id,parentId as pId, moduleName as name  from dictionary_type  
        where parentId = #{id}
    </select>  
    
    <select id="getDictionaryAll" resultType="hashMap">
    	select id as id,parentId as pId, moduleName as name  from dictionary_type  
    </select>
    
    <select id="getDictionaryById" resultType="hashMap" parameterType="int">
    	select moduleName as name from dictionary_type where id = #{id}
    </select>
</mapper>  